(()=>{'use strict';
const C=window.PORTAL_CONFIG,sb=window.supabase.createClient(C.workforceUrl,C.workforceKey,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}}),st=document.getElementById('status'),form=document.getElementById('loginForm'),submit=document.getElementById('loginSubmit'),magic=document.getElementById('magic'),SITE_KEY='0x4AAAAAAE4-F43E-viFsKat';
let turnstileId=null,token='',ready=false;const set=(m,b=false)=>{st.textContent=m||'';st.style.color=b?'#a72d2d':'#17764a'},sync=()=>{if(submit)submit.disabled=!token},reset=()=>{token='';sync();if(window.turnstile&&turnstileId!==null)try{window.turnstile.reset(turnstileId)}catch(_){}};
function mount(){if(ready||!window.turnstile)return;ready=true;turnstileId=window.turnstile.render('#turnstileLogin',{sitekey:SITE_KEY,action:'faa_login',theme:'auto',size:'flexible',callback:t=>{token=String(t||'');sync()},'expired-callback':()=>{token='';sync();set('Security check expired. Please complete it again.',true)},'timeout-callback':()=>{token='';sync();set('Security check timed out. Please try again.',true)},'error-callback':code=>{token='';sync();set(`Cloudflare security check could not load${code?` (${code})`:''}.`,true);return true}})}
(function wait(){if(window.turnstile)mount();else setTimeout(wait,80)})();
async function route(){const {data:{session}}=await sb.auth.getSession();if(!session)return false;const r=await fetch(`${C.workforceUrl}/functions/v1/workforce-session-context`,{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${session.access_token}`,'apikey':C.workforceKey},body:JSON.stringify({requested_portal_code:C.portalCode})});const d=await r.json().catch(()=>({}));if(r.status===401)return false;if(!r.ok){set(d.reason==='exact_portal_not_authorized'?'This account does not have access to this portal.':d.error||'Portal access denied.',true);return true}if(d.requires_workspace_selection){location.replace('/workspace.html');return true}if(d.membership?.id)localStorage.setItem(`s4u_${C.portalCode}_membership`,d.membership.id);location.replace('/dashboard.html');return true}
form.onsubmit=async e=>{e.preventDefault();if(!token){set('Complete the security check to continue.',true);return}set('Signing in…');submit.disabled=true;const f=new FormData(e.currentTarget);const {error}=await sb.auth.signInWithPassword({email:String(f.get('email')||'').trim(),password:String(f.get('password')||''),options:{captchaToken:token}});if(error){set(error.message,true);reset();return}await route()};
magic.onclick=async()=>{const email=prompt('Email address');if(!email)return;if(!token){set('Complete the security check before requesting a sign-in link.',true);return}magic.disabled=true;const {error}=await sb.auth.signInWithOtp({email:String(email).trim(),options:{emailRedirectTo:`https://${C.domain}/auth-handoff.html`,captchaToken:token}});magic.disabled=false;set(error?error.message:'Check your email for the sign-in link.',!!error);reset()};sync();route();
})();