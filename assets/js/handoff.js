(async()=>{const C=window.PORTAL_CONFIG,sb=window.supabase.createClient(C.workforceUrl,C.workforceKey,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});for(let i=0;i<30;i++){const {data:{session}}=await sb.auth.getSession();if(session){location.replace('/dashboard.html');return}await new Promise(r=>setTimeout(r,150))}document.getElementById('msg').textContent='Secure sign-in did not complete. Please sign in.';document.getElementById('login').style.display='inline-flex'})();